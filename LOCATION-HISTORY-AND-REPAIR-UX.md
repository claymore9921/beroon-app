# تغییرات خرابی و تاریخچه موقعیت

- صفحه خرابی مدیر شعبه به دو مسیر روشن «دستگاه خراب» و «تحویل» تقسیم شده است.
- بدون جستجو، دستگاه‌های فعالِ حاضر در محل فعلی شعبه نمایش داده می‌شوند.
- با جستجو یا اسکن QR، دستگاه از کل ناوگان پیدا می‌شود؛ بنابراین حتی اگر مالک یا current_branch_id آن شعبه دیگری باشد، مدیر می‌تواند خرابی را ثبت و آن را به تعمیرگاه ارسال کند.
- ثبت خرابی، شعبه‌ای که دستگاه را واقعاً در اختیار داشته به‌عنوان گزارش‌دهنده در سابقه خرابی نگه می‌دارد و موقعیت دستگاه به تعمیرگاه تغییر می‌کند.
- جدول scooter_location_events اضافه شده و آخرین موقعیت‌های دستگاه را نگه می‌دارد.
- migration جدید تا حد ممکن سابقه قبلی را از آمار شب، چک‌لیست صبح، حمل‌ونقل‌ها و موقعیت فعلی دستگاه backfill می‌کند.
- صفحه ادمین «موقعیت دستگاه» آخرین ۱۰ موقعیت ثبت‌شده را با تاریخ شمسی و ساعت تهران نشان می‌دهد.

پس از جایگزینی پروژه:

    mix format
    mix compile
    mix ecto.migrate
    mix phx.server
