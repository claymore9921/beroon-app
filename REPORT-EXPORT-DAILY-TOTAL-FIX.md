# اصلاح نهایی خروجی `/report-export`

این نسخه مستقیماً اکشن `download_admin_report_export/2` و تابع تولید فایل `evening_inventory_xls/1` را اصلاح می‌کند.

## ستون‌های خروجی

- تمام شعب فعال، به تفکیک نوع دستگاه
- جمع کل شعب
- ارسال‌شده و در انتظار پذیرش تعمیرگاه (`needs_service`)
- پذیرش‌شده و در انتظار تعمیر (`awaiting_repair`)
- در حال تعمیر (`repairing`)
- ترخیص‌شده و هنوز تحویل شعبه نشده (`ready_for_pickup`)
- جمع تعمیرگاه
- در انتظار قطعه
- دستگاه‌های امانی
- انبار دستگاه‌های نو
- فروش‌رفته‌ها
- جمع ناوگان روز

## جمع کل روزانه

عدد نهایی از این فرمول ساخته می‌شود:

`جمع اسکن صحیح شعب + جمع تعمیرگاه + در انتظار قطعه + امانی`

انبار نو و فروش در ستون‌های مستقل نمایش داده می‌شوند، اما در جمع ناوگان عملیاتی وارد نمی‌شوند.

برای جلوگیری از دوباره‌شماری، دستگاهی که در زمان خروجی در وضعیت تعمیرگاه، در انتظار قطعه یا امانی است از جمع اسکن شعب حذف و فقط در ستون وضعیت فعلی خودش شمرده می‌شود.

اسکن‌های متعلق به شعبه دیگر و اسکن‌های حمل‌ونقل نیز در جمع شعب محاسبه نمی‌شوند.

هیچ migration جدیدی برای این اصلاح لازم نیست.
