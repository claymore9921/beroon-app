# خروجی همه دستگاه‌های تخصیص‌یافته به شعب

این اسکریپت گزارش امروز را بر اساس ساعت تهران و تاریخ شمسی تولید می‌کند.
ستون‌های شعب از آمار شبانه خوانده نمی‌شوند و مستقیماً از جدول `scooters` و مقدار `branch_id` ساخته می‌شوند.

## تفاوت این نسخه

- بخش و ستون‌های تعمیرگاه از فایل حذف شده‌اند.
- هر دستگاهی که `branch_id` دارد، بدون توجه به وضعیت آن، زیر شعبه مالک شمرده می‌شود.
- وضعیت‌های زیر نیز از شمارش شعب حذف نمی‌شوند:
  - فعال
  - خراب یا نیازمند تعمیر
  - ارسال‌شده به تعمیرگاه
  - پذیرش‌شده
  - در حال تعمیر
  - آماده تحویل
  - در انتظار قطعه
  - حمل‌ونقل
  - امانی
  - سرقتی
  - از مدار خارج‌شده
- در نتیجه «جمع کل دستگاه‌های تخصیص‌یافته» فقط مجموع رکوردهای دستگاه دارای شعبه مالک است و به اسکن شبانه وابسته نیست.

ستون‌های «در انتظار قطعه»، «امانی» و «سرقتی» صرفاً برای اطلاع از وضعیت نمایش داده می‌شوند و دوباره به جمع کل اضافه نمی‌شوند.

## اجرا در PowerShell

از ریشه پروژه اجرا کنید:

```powershell
.\export-assigned-inventory.ps1
```

یا مستقیم:

```powershell
mix run priv/scripts/export_assigned_inventory_report.exs
```

برای محیط production:

```powershell
$env:MIX_ENV="prod"
mix run priv/scripts/export_assigned_inventory_report.exs
```

در Linux یا سرور production:

```bash
MIX_ENV=prod mix run priv/scripts/export_assigned_inventory_report.exs
```

## محل خروجی

فایل در پوشه `exports` ساخته می‌شود و نام آن مشابه نمونه زیر است:

```text
exports/beroon-all-assigned-inventory-1405-05-12.xls
```
